whisper:
  model_size: "base.en"
  language: "en"
  device: "cpu"
llm:
  provider: "openai"
  model: "gpt-4o-mini"
  max_tokens: 256
  temperature: 0.0
pipeline:
  confidence_threshold: 0.7
  enable_fallback: true
  enable_confirmation: false
[project]
name = "g1-voice-control"
version = "0.1.0"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["schema*", "parser*", "pipeline*", "stt*", "config*", "demo*", "benchmark*"]
